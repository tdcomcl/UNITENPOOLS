### Base de Datos
# SQLite (por defecto, comentado si usas PostgreSQL)
# DB_TYPE=sqlite
# DB_PATH=piscinas.db

# PostgreSQL (descomenta y configura si migras a PostgreSQL)
DB_TYPE=postgresql
DB_HOST=localhost
DB_PORT=5432
DB_NAME=unitenpools
DB_USER=unitenpools_user
DB_PASSWORD=nexo6445

### SMTP / Notificaciones (Mailcow)
SMTP_HOST=10.10.10.176
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=notificaciones@tdcom.cl
SMTP_FROM=notificaciones@tdcom.cl
SMTP_PASS=CAMBIAR_ESTE_PASSWORD
ALERT_EMAILS=crt@tdcom.cl,franciscaasenciomaleno1@gmail.com

# TLS (si te conectas por IP y el cert es para hostname, puede dar mismatch)
SMTP_TLS_REJECT_UNAUTHORIZED=false
# Si tienes el hostname del certificado, puedes fijarlo acá:
# SMTP_TLS_SERVERNAME=mail.tdcom.cl

### Odoo (opcional por variables de entorno)
# ODOO_URL=http://10.10.10.166:8086
# ODOO_DB=pools
# ODOO_USERNAME=admin
# ODOO_PASSWORD=admin

# Contraseña para importar archivos Excel (protección adicional)
EXCEL_IMPORT_PASSWORD=import2024